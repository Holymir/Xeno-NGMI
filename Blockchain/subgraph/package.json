{
  "name": "xeno",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ xeno",
    "create-local": "graph create --node http://localhost:8020/ xeno",
    "remove-local": "graph remove --node http://localhost:8020/ xeno",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xeno"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0",
  }
}
